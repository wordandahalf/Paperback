package image.dithering

import image.Color
import java.awt.image.BufferedImage

object FloySteinbergDiffusion : DitheringDiffusion {
    override fun diffuse(x: Int, y: Int, error: Color, image: BufferedImage) {
        TODO("Not yet implemented")
    }
}